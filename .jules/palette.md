## 2026-05-28 - Bilingual Accessibility in Documentation
**Learning:** For bilingual documentation (English/Bengali), using semantic HTML `<div>` tags with `lang` attributes (`lang="en"`, `lang="bn"`) is critical for screen reader accessibility.
**Action:** Always wrap language-specific content in `<div>` tags with the appropriate `lang` attribute.
