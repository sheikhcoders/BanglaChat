## 2026-06-10 - [Accessibility in Sparse Repositories]
**Learning:** In repositories without an initialized UI (no source code), micro-UX improvements can still be applied to project entry points like READMEs and PR templates to ensure immediate accessibility for contributors.
**Action:** Use semantic `<span lang="...">` tags for translations in documentation to support screen readers even before the main application UI is built.
