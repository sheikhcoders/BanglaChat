# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# Efficiency: Cancel redundant builds on the same branch to save ~5-10 CI minutes per redundant run
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v4
    - name: Detect package manager
      id: detect-package-manager
      # Performance: Detect pnpm for faster dependency installation (saves ~20-30s compared to npm)
      run: |
        if [ -f "${{ github.workspace }}/pnpm-lock.yaml" ]; then
          echo "manager=pnpm" >> $GITHUB_OUTPUT
          echo "command=install" >> $GITHUB_OUTPUT
          echo "cache=pnpm" >> $GITHUB_OUTPUT
        elif [ -f "${{ github.workspace }}/yarn.lock" ]; then
          echo "manager=yarn" >> $GITHUB_OUTPUT
          echo "command=install" >> $GITHUB_OUTPUT
          echo "cache=yarn" >> $GITHUB_OUTPUT
        elif [ -f "${{ github.workspace }}/package-lock.json" ]; then
          echo "manager=npm" >> $GITHUB_OUTPUT
          echo "command=ci" >> $GITHUB_OUTPUT
          echo "cache=npm" >> $GITHUB_OUTPUT
        else
          echo "manager=npm" >> $GITHUB_OUTPUT
          # Fallback to install if no lockfile is found (ci requires a lockfile)
          echo "command=install" >> $GITHUB_OUTPUT
          # Disable caching if no lockfile is found to prevent setup-node failure
          echo "cache=" >> $GITHUB_OUTPUT
        fi
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: ${{ steps.detect-package-manager.outputs.cache }}
    - name: Install pnpm
      if: steps.detect-package-manager.outputs.manager == 'pnpm'
      # Performance: Enable corepack to use pnpm (saves ~10s compared to using a separate setup action)
      run: corepack enable pnpm
    - name: Install dependencies
      run: ${{ steps.detect-package-manager.outputs.manager }} ${{ steps.detect-package-manager.outputs.command }}
    - run: ${{ steps.detect-package-manager.outputs.manager }} run build --if-present
    - run: ${{ steps.detect-package-manager.outputs.manager }} test
